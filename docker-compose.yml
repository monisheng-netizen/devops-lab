version: '3'

services:
 web:
  image: nginx
  ports:
   - "8082:80"
  volumes:
   - ./index.html:/usr/share/nginx/html/index.html

 db:
  image: mysql
  environment:
   MYSQL_ROOT_PASSWORD: example

